**/node_modules
**/.next
**/dist
**/.turbo
.git
docs
infra
**/*.log
**/*.err
**/.env
**/.env.*
!.env.example
!.env.production.example

# 运行期数据绝不进构建上下文。
# 曾经的事故：apps/api/uploads 被烤进生产镜像 22MB，内含单证原件、HZB 案例合同与
# GDPR 导出包（自然人个人数据）——拿到镜像即拿到这些文件。生产环境这些数据
# 归属 named volume（UPLOAD_DIR=/data/uploads），任何情况下都不该来自构建上下文。
**/uploads
HZB

# 测试与构建产物
**/coverage
**/*.tsbuildinfo
**/.jest-cache
